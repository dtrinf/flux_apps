apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: flux-apps
  namespace: apps
spec:
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg